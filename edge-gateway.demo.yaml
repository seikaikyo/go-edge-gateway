gateway:
  name: "edge-gw-demo-01"
  log_level: info
  health_port: 8080

coordinator:
  url: "https://dashai-go.onrender.com"
  node_id: "edge-gw-demo-01"
  location: "Demo Environment"
  heartbeat_interval: "30s"

uplink:
  type: coordinator
  batch_interval: "5s"
  max_batch_size: 100

plugins:
  secsgem:
    enabled: false

  modbus:
    enabled: false

  mqtt:
    enabled: false
